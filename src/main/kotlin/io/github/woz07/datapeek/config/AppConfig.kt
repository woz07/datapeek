package io.github.woz07.datapeek.config

//import github.woz07.liteconfig.Configuration
import java.io.File

object AppConfig {
    // private val config = Configuration(File("config.txt"))

    // TODO: Need to continue setting this up
    //  - Data needs to be stored in APPDATA
    //  - Utilize c program (.exe) for opacity of app
    //  - Need to rebuild opacity.c as it has no .exe (prev one not reusable as dif target name)
    //  - Also check how macrox handles configuration data, it's very interesting and pretty neat and I like it, reuse that
}